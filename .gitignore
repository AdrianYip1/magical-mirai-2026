# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Env (.env holds the TextAlive token which is committed so judges can build from source.
# It's a client token that ships to the browser anyways)
.env.local
*.local

# Claude
.claude/
CLAUDE.md
CLAUDE.local.md

# Docs (planning files, not for submission)
docs/

# Source fonts. The app only loads MPLUS1-Black.typeface.json (committed); other
# formats stay ignored so stray large fonts don't get committed.
public/*.otf
public/*.ttf
public/*.woff2

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db